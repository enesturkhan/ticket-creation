/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['api.qrserver.com'],
  },
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
